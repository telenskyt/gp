% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/suppress_warnings.R
\name{suppress_warnings_from}
\alias{suppress_warnings_from}
\title{Suppress specific warnings by message and stack}
\usage{
suppress_warnings_from(
  expr,
  message = NULL,
  fun = NULL,
  package = NULL,
  fixed = TRUE
)
}
\arguments{
\item{expr}{Expression to evaluate.}

\item{message}{Optional character pattern matched against \code{\link[=conditionMessage]{conditionMessage()}}.
If \code{NULL}, the warning message is not checked.}

\item{fun}{Optional function name that must appear in the trimmed warning
stack. If \code{NULL}, the stack function name is not checked.}

\item{package}{Optional package or environment name that must appear in the
trimmed warning stack, for example \code{"Matrix"} or \code{"R_GlobalEnv"}.
If \code{NULL}, the stack package/environment is not checked.}

\item{fixed}{Logical scalar passed to \code{\link[=grepl]{grepl()}}. Use \code{TRUE} for literal
message matching and \code{FALSE} for regular expressions.}
}
\value{
The value of \code{expr}, with matching warnings suppressed.
}
\description{
Runs an expression and suppresses only warnings that match all supplied
criteria. Criteria may include warning text and whether the trimmed warning
stack contains a package/environment name or function name.
}
\details{
The stack is trimmed at the warning's \code{\link[=conditionCall]{conditionCall()}} before matching.
If all filters are \code{NULL}, this suppresses all warnings from \code{expr}.

If both \code{fun} and \code{package} arguments are supplied, they must match at the same frame of the stack.
}
\examples{
f <- function() {
	warning("f warned")
}

suppress_warnings_from(
	f(),
	message = "f warned",
	package = "R_GlobalEnv",
	fun = "f"
)

}
