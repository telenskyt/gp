% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gpPredictFromLatent.R
\name{gpPredictFromLatent}
\alias{gpPredictFromLatent}
\title{Convert latent GP predictions to the requested prediction type}
\usage{
gpPredictFromLatent(
  gp,
  pred,
  data,
  type = c("latent", "terms", "response"),
  hyperpar = gpHyperparList(gp),
  se.fit = FALSE,
  cov.fit = FALSE,
  pred.cov = NULL,
  conf.int = FALSE,
  conf.level = 0.95
)
}
\arguments{
\item{gp}{Object of class \code{gp}.}

\item{pred}{Matrix or data frame with column \code{f}, and optionally \code{f_SE}.
The rows must initially be in the dimension of \code{gp$GP_factor}.}

\item{data}{Object of class \code{gpData} to pass to the likelihood template.
The caller must provide data on the scale expected by the template.}

\item{type}{Character; prediction type. Same meaning as in \code{predict.gp()}.}

\item{hyperpar}{List of hyperparameter values, usually from \code{gpHyperparList(gp)}.
When likelihood    hyperparameters are present in the model (fixed or fitted),
it must already contain the \code{.lik} component with correct hyperparameter values.}

\item{se.fit}{Logical; should standard errors be present in the returned
prediction matrix? If \code{TRUE} and \code{pred} has no \code{f_SE} column, \code{pred.cov}
must be supplied.}

\item{cov.fit}{Logical; should the latent covariance matrix be returned?}

\item{pred.cov}{Optional covariance matrix of latent predictions, in the same
row order as \code{pred} before any likelihood-template reindexing.}

\item{conf.int}{Logical; should latent-scale confidence interval columns be
added? Same meaning as in \code{predict.gp()}.}

\item{conf.level}{Numeric confidence level used when \code{conf.int = TRUE}.}
}
\value{
If \code{cov.fit = FALSE}, a prediction matrix. If \code{cov.fit = TRUE}, a list
with components \code{pred} and \code{cov}, matching \code{predict.gp()}.

For \code{type = "latent"}, predictions remain in the dimension of \code{gp$GP_factor}.
For \code{type = "terms"} or \code{type = "response"}, predictions are reindexed to the
main table when \code{gp$lik.reindex2main} requires it. In that reindexed case,
\code{cov.fit = TRUE} is currently not supported, matching \code{predict.gp()}.
}
\description{
\code{gpPredictFromLatent()} is an internal helper used by prediction functions
that have already computed latent predictions for \code{f}. It applies the common
\code{type = "latent"}, \code{"terms"}, and \code{"response"} prediction logic used by
\code{predict.gp()}.
}
\keyword{internal}
